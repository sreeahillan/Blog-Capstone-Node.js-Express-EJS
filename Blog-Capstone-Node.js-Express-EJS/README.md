# Blog-Capstone-Node.js-Express-EJS
A full-stack blog app with complete CRUD (create, edit, delete posts) using Express routes and EJS templating. Form data handled via body-parser, posts stored in-memory, styled with custom CSS. Demonstrates route params, server-side rendering, and RESTful route design.
